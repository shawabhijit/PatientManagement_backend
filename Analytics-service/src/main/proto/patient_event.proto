syntax = "proto3";

package patient.events;
// this line tells to generate multiple files for diff .proto files
option java_multiple_files = true;

message PatientEvent {
  string patientId = 1;
  string name = 2;
  string email = 3;
  string event_type = 4;
}